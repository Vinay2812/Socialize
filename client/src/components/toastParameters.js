import { Zoom } from "react-toastify";

export const toastParameters = {
    position: "top-right",
    autoClose: 5000,
    pauseOnHover: true,
    draggable: true,
    theme: "dark",
    transition: Zoom
}